{
  "extends": [
    "eslint:recommended",
    "plugin:prettier/recommended" // Extends the recommended Prettier config
  ],
  "plugins": ["prettier"], // Adds the Prettier plugin
  "rules": {
    "prettier/prettier": "error" // Displays Prettier errors as ESLint errors
  }
}
